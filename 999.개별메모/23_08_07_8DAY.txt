[ 오늘의 일정 ]

8월7일(월)
------------

1. 도깨비 메인 설계도 마무리

2. 테이블 풀 팩키지 : 드라마 PJ

3. CSS학습
1) 블록가로크기/세로크기
2) 인라인 플롯
3) 포지션

4. 포지션 연습 : 로봇PJ 1편


프로젝트에 대한 이해도가 높으면 더 잘할 수 있음

...1교시...
발사믹 와이어프레임 복습

...2교시...
발사믹 다듬기
미디어 쿼리 등을 진행하는 시점에 대한 설정(이전엔 % 몇 이후는 미디어쿼리)

...3교시4교시...
드라마 PJ
라디오버튼 이름을 동일하게 쓰면 그룹설정이 되서 하나만 클릭됨
라벨 for와 input의 id가 동일하면 세트가 됨

라벨아래에 형제요소로 div박스를 넣고 그 안에 table을 넣어서 한쪽만 보이게 하면 
#rdo2:checked ~
/* #rdo2가 체크 되어있으면 */
.drama>table:first-child {
    display: none;
}
+는 바로 다음에 있는 형제를 선택하는 선택자
.rdo:checked+.menu

데이터 전송할때 name의 요소를 보냄
Ajax에서는 id도 보낼 수 있음

라벨은 inline요소여서 inline-block 만들고 위치 49%에 위치
(이유는 엔터간극 4px때문에 50%맞추면 가운데 안오고 아래로 떨어짐)

...5교시...
포지셔닝과 젠코딩
.bx#s${s$}[href=""]*3
 
    [ 젠코딩(에밋코딩) ]
    .bx#s${s$}*3
    __________________
    . : class
    # : id
    $ : 증가숫자
    *n : 반복생성횟수
    {} : 데이터출력
    [] : 속성세팅값
    > : 하위직계자식요소
    + : 형제요소
    () : 요소구분하기

[ CSS Position ]
- 위치를 효과적으로 배치하기 위한
    코딩법을 제공하는 속성

    [ 1. static 포지션 ]
    1. 기본값
    2. 위치이동 속성이 적용되지 않음
    (top, left, bottom, right 등)
    @미디어쿼리에서 자리찾아올때 이동함
    3. 위치이동은 margin 속성을 사용함
    >특징 : 마진으로 이동하면 
        아래 또는 옆 다른 static요소에
        영향을 미쳐서 같이 이동됨
    
    [ 위치 이동 속성 ]
    - 선택요소의 위치를 이동시켜주는 설정 속성
    - 포지션에 따라서 다르게 적용됨
    - 종류는 top, bottom, left, right

    사각형 안쪽으로 움직이면 + 밖으로 움직이면 -
    경계는 t l r b 기준

    [ 2. relative 포지션 ]
    -렐러티브!
    1. 위치이동 속성이 적용된다
    2. 위치이동의 기준은 원래 있던 위치
        (그래서 상대적인 위치)
    3. 이동시 다른 요소에 영향이 없이 겹쳐질 수 있다.
    4. 원래 있던 위치는 그대로 보존된다.
        (기존 위치가 보존되기 때문에 본래의 디자인이 망가지지 않는다.)
    5. 위치값 설정시 2가지 기준으로만 셋팅한다
    - 위, 아래 중 하나
    - 좌, 우 중 하나
    top, left / bottom, right
    top, right / bottom, left
    box기준으로 안쪽이 + 밖이 -

    6. 마진은 그대로 적용된다!
    (다른 static한 요소에 영향을 미친다)
    -> 본래 위치에 마진이 적용 된 것이므로
    상대적인 이동값도 변경 된 것으로 보인다!

    7. relative는 위치 이동값을 사용하지 않으면
    static과 똑같은 특징을 가진다

    8. relative는 겹쳐질 수 있으므로
    위 아래 관계가 성립됨(z index)
    (1) static보다는 항상 위
    (2) static이 아니면 나중에 그린게 위!
        (손바닥놓기)
    (3) z-index 속성으로 숫자가 크면 위!

    9. 부모자격
    __________________________________
    [ z-index란? ] -static이 아닌 포지션만 적용
    - 겹치는 포지션을 준 경우 위치순서를 정하는 디스플레이를 정하는 속성
    - z 는 화면방향의 층을 의미
    - 기본값은 0 -> 같은 층에 있는 모든 요소는 0
    
    ->기본값은 0이므로 마이너스 값은 static요소보다 아래로 내려감
        (이건 body보다 내려가서 선택이 안됨)
    **주의 : 
        z-index비교시에 형제레벨 먼저 비교 -> 하위자손 비교
        여러부분 z-index 주면 위아래 관계가 복잡해져서 정리가 어렵다

    
    absolute가 처음에 지정하면 젠틀함~ 
    앱솔루트는 위치값을 쓰기 전과 후로 나뉨

    웹페이지에서 땅은 위쪽 왼쪽임 중력방향이 위쪽 주소창이라 생각하면 됨

    absolute 위치값 없으면 마진도 지키고 s1 아래에 위치함
    젠틀 앱솔루트는 처음에는 padding만큼 밀리고 content만큼 밀리고 위치함.
    /* .a3는 젠틀앱솔루트박스
        부모는 아니지만(포지션이 부모가 불가능함) 박스안에 있는다. */
    세부조정은 위치값이 아니고 margin값으로 움직여야함
    absolute는 relative만 부모자격이 있는게 아니고 최대한 static한 relative를 이용함