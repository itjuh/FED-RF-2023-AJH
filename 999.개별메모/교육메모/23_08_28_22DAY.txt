[ 오늘의 일정 : 8월28일(월) ]
------------------------------

1. JS학습
      1) 제어문 연습 : for문옵션
      2) 배열과 객체 : 객체편

2. HTML+CSS 기초 및 응용
      1) Robot PJ : 
      - 마블 페이지 구성 및 상세코딩
      - 체크사항 : 모바일 배경fixed(robot.html)

3. 프로젝트 구현 및 리뷰 시간

-------------------------------

...1교시...
어워즈~
for문 옵션
getElementById / querySelector 두개는 값을 바로 가져옴
그외에 리스트를 넣으면 NodeList 컬렉션에 담김

...2교시...
for문 옵션

함수를 할당하면 명령문임, 그래서 ; 씀
선언적 함수는 ;안써~!! 명령문도 안써~~~~
let 블록영역 지역변수! 
btns.length 를 for문안에 돌리면 계속 불러와야해서 시스템이 느려짐

jquery는 씨블링스로 자식요소 선택이 쉬움
하지만 우리는 모르니까 부모로 올라가서 자식요소의 .forCls를 선택하면 됨

...3교시...
break; : 제어문을 나감
return; : 함수를 나감

객체만들기~
객체를 만드는 객체를 사용해서 ??????
구조화 : 각 부분의 구성을 정의
정보를 의미있는 정보로 셋업하는 것을 객체라고 함
데이터베이스의 스키마와 동일 정보를 어디로 넣을지 구분하는 것
object는 frontend의 DB같은 존재

document 의 아버지는 window!!

[ 객체의 선언의 2가지 방식 ]
1. new 키워드 선언방식
- new Object()

2. 리터럴 선언방식(객체리터럴)
- 변수 = {} 

이름으로 부르면 객체! 번호로 부르면 배열


{}
let,const의 블록영역
코드담는 영역
{}데이터 담는 셋업공간 == 객체!

객체 셋팅하기!!
const SSG = new Object({});

[ 객체의 호출 ]
- 객체명["속성명"] , 객체명.속성명

...4교시...

JSON은 문자데이터는 "" 꼭 싸주자

변수형으로 쓸 수 있는 데이터는 ""안싸도 됨
ex)
const 객체 = new Object({
    "1 번":1,
    2번:2
});
// alert에 ,사용 불가 +만 가능

...5,6교시...
const showMovieInfo = function(){};
호이스팅 안됨! const!!

body에 값을 넣었다 뻈다하면
클릭링크가 사라져서 새로 box 만들어서 진행함
document.body.classList.remove('on');
